---
applications:

- name: FunnyQuotesBasicPush
  random-route: true
  memory: 512M
  health-check-type: http
  health-check-http-endpoint: /
  buildpack: dotnet_core_buildpack
  services:
    - eureka
    - hystrix
    - config-server
